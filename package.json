{
	"name": "resume-maker",
	"version": "1.0.0",
	"description": "Responsive resume service landing page",
	"private": true,
	"scripts": {
		"dev": "tailwindcss -i ./src/input.css -o ./dist/output.css --watch",
		"build": "tailwindcss -i ./src/input.css -o ./dist/output.css --minify",
		"preview": "npm run build && npx serve .",
		"clean": "rimraf dist && mkdir dist"
	},
	"author": "Your Name",
	"license": "MIT",
	"devDependencies": {
		"autoprefixer": "^10.4.20",
		"postcss": "^8.4.41",
		"tailwindcss": "^3.4.13",
		"rimraf": "^5.0.5",
		"serve": "^14.2.2"
	}
}
